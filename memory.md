# 项目关键信息

- 项目名称：PA Agent（Price Action AI K 线分析桌面端）
- 技术栈：Python 3.11+、PyQt6、pyqtgraph；依赖由 `pyproject.toml` 和 `uv.lock` 管理。
- 推荐本地启动：在项目根目录执行 `uv run python run.py`。
- 主入口：`run.py`，实际 GUI 入口为 `pa_agent.main:main`。
- 本地配置：运行时配置位于 `config/`；仓库仅提交 `*.example.json`，密钥和个人配置不应提交。
- 日志目录：`logs/`，重点查看 `logs/pa_agent.log` 与 `logs/crash.log`。
- 应用性质：本地桌面 GUI，不提供固定 HTTP 端口；运行验收以 GUI 进程、主窗口和启动日志为准。
- AI 后端：支持 OpenAI 兼容 API、Cursor SDK 和官方 `openai-codex` Python SDK；Codex 路由使用本机登录、临时线程和只读沙箱。

## 维护记录

- 2026-08-15：量化工作台新增单窗口“智能选股”业务区。确定性版本 `a_share_stock_selection_v1` 通过近期热点题材、主力关注题材、量能窒息和趋势启动四条通道扫描全A候选种子；量能窒息固定为近5日相对前20日缩量并伴随ATR/振幅收敛且趋势结构未破坏，趋势启动固定为MA20>MA60、MA20斜率向上、接近/突破20日高点并放量。所有阈值写入 `settings.json.stock_selection`。每只入选股票必须有及时、可回溯的公告快照且没有官方确认的重大负面事件；公告为空、过期或无法核验时按 `DATA_INCOMPLETE` 失败关闭。候选可一键加入我的监控池并进入既有独立沙箱，池外股票仍走 `manual_exception_4321_v1`，选股不产生交易授权。
- 2026-08-15：首页原有的“AI 模型设置”“飞书发送通知设置”“其他通用设置”三个顶层入口统一收进“系统设置”下拉菜单；三个原对话框及保存逻辑保持不变，并为后续系统级配置保留统一扩展入口。
- 2026-08-14：OOS收盘采集增加15:00日线恢复窗口。正常15分钟行情、情绪和股票池监控仍只接受每根闭合K线后的5分钟；若15:00后前5分钟网络异常，15:05～15:14只允许补采当天15:00日线，不回填过期15分钟K线、情绪或股票池监控账本。15:15后停止恢复，周末不运行；当日日线已完整时运行时不会重复请求，恢复异常按 `daily_*` 分类并继续失败关闭。
- 2026-08-14：异步热点批次增加请求纪元与股票池版本绑定。热点请求启动时冻结 `validation_epoch_id + pool_version`，返回保存前再次与当前纪元核对；成员变更前启动、变更后返回的旧批次只保留为一般风险信息并写入 `hotspot_request_validation_epoch_mismatch`，不得被重新标记为新纪元热点，也不得进入新池4:3:2:1评分或OOS晋级。热点冻结时间早于纪元激活、采集早于冻结等时间倒置同样不绑定纪元。
- 2026-08-14：热点与OOS数据完成验证纪元逐记录隔离。私人池热点必须在纪元激活后采集并携带匹配的纪元ID、成员哈希和池版本；同成员刷新版本可共同归属一个纪元，成员增删后旧热点与旧OOS记录不得进入新纪元。动态OOS包导出和导入会逐条校验上述字段；旧固定11股数据包保留无纪元清单的审计兼容，不得给当前私人池授予晋级资格。
- 2026-08-14：股票池增删后的验证隔离升级为 `validation_epoch_v1`。每个成员集绑定独立纪元、成员哈希、激活时间和OOS观察命名空间；普通行情刷新不新建纪元，成员新增/删除必定新建并保留旧纪元审计。OOS采集、导出清单、组合回测、影子证据和晋级均校验当前纪元，旧固定11股成绩不能授予当前9股池资格。策略验证页显示当前纪元、绑定池版本、成员哈希、65日热身和首个可评分条件。
- 2026-08-14：动态私人股票池补齐逐股15分钟监控链路。每个股票池版本使用 `ashare_private_pool_monitor_v1-*` 独立只追加账本，新增A股从下一根完整15分钟K线开始采集并投影到首页沙箱；旧版本行情保留但不会进入新版本。该监控账本与冻结的 `cloud_ai_topdown_4321_intraday_v1` OOS晋级数据隔离，股票池增删不会污染原策略样本外证据。
- 2026-08-14：交易股票池支持在当前量化工作台新增和移除A股。首次成员变更以固定11股为种子生成 `ashare_private_pool-*` 新版本，此后运行时以最新私人版本为当前池，不会被月度固定池重新覆盖。新增支持6位代码或完整A股名称，必须通过证券身份、20日行情、上市120日、ST/退市和涨跌停准入检查；移除前检查同花顺持仓、未完成委托、开放计划和待对账记录。每次变更保存父版本、成员哈希和变更摘要，旧快照、信号、交易与绩效不删除；成员变化后策略留在CANDIDATE或转为PAUSED，必须重新完成OOS和影子验证。
- 2026-08-14：生产范围固定为A股股票。主窗口仅开放东方财富A股数据源，默认 `600519 / 15m`；指数仅参与4:3:2:1市场评分，非A股、指数、基金、债券等不得进入分析投资、组合授权或同花顺预填。股票池模型和数据库写入层也会失败关闭，混入任何非A股或非股票标的时整份股票池拒绝保存；历史多市场代码只保留兼容用途。
- 2026-08-14：生产OOS审核按65个完整日线交易日进行真实热身，不允许事后回填冻结前行情；情绪、热点和执行元数据从热身后的可评分15分钟时段开始强制完整。日线记录同时冻结停牌、涨跌停锁死、ST、退市、上市天数和风险行业元数据；候选评分不再重复写入信号条件下的OOS行情。冻结后早期缺少 `observed_at` 的准备记录保留在原始账本，导出时按 `scoreable_observations_v1` 确定性排除并记录数量，热身后的时间缺口仍硬阻断。
- 2026-08-14：池外A股专业评估生产链路已接通。查询会先重新扫描当前基础池并使用真实市场宽度，不修改固定11股；日线通过后才进入热点与连续两根已收盘15分钟评分。池外计划固定为 `manual_exception_4321_v1`、半风险、最多1只、单次批准、禁止加仓补仓追价，绩效与主策略隔离；非A股在数据请求前拒绝。查询本身只保存确定性评估，不直接授权或生成订单。
- 2026-08-14：股票详情名称查询不再复用TradingView多市场别名，改为优先匹配当前基础池、再调用东方财富A股搜索，并过滤同名港股、基金与指数；多个A股候选时要求用户输入6位代码。由此避免“中国移动”等A/H同名股票被错误解析成港股后再拒绝。
- 2026-08-14：修正北交所东方财富路由。北交所旧 `4/8` 开头代码和新 `92xxxx` 代码均使用北交所兼容行情市场与 `BJ/.BJ` F10标识，不再因首位是9而误路由到沪市；当前并行科技仍保持只分析边界。
- 2026-08-14：池外计划的半风险、单次批准和最多1只限制已下沉到组合风控。风控按 `manual_exception_4321_v1` 策略身份强制匹配池外通道，不信任调用方自行声明；同时校验四层评分的策略版本、评分版本、股票池版本和股票代码，错配时失败关闭。
- 2026-08-14 14:31：冻结后OOS账本已有固定成分1条、15分钟行情168条（14个标的、12个完整时点）、市场情绪11条和热点987条；14:30时点包含10只可授权A股与4个评分指数，逐股执行元数据和真实 `observed_at` 均已保存。尚未收盘，因此日线热身仍为0/65；首个真实日线只能在15:00收盘并通过数据时效校验后追加，禁止提前补造。

- 2026-08-11：首次在当前工作区进行本地启动检查并创建本文件。
- 2026-08-11：集成 Codex SDK Agent 分析后端，默认 Codex 模型为 `gpt-5.6-terra`。
- 2026-08-14：生产环境所有大模型访问统一收口到 `CodexSdkClient`；两阶段分析、自由对话和校验重试禁止切换到 OpenAI 兼容 API、Cursor、QClaw 或 WorkBuddy。默认 `gpt-5.6-terra`、推理等级 `high`，使用本机 Codex 登录，PA Agent 不保存模型 API Key。
- 2026-08-14：首页行情分析调整为全股票池实时控制面。固定池每只A股拥有独立、可持久化的交易状态沙箱；后台按日线扫描、热点5分钟刷新和闭合15分钟四层评分推进状态。首次进入 `quant_tradeable` 时按股票、闭合时点和计划去重发送飞书提醒；通知失败不改变确定性评分。账户风控与同花顺继续作为信号之后的执行层，且最终委托仍由用户确认。

## 量化交易设计原则

> 不要急着增加更多策略，也不要急着为每个品种单独调参；先修复止损逻辑、建立交易结果闭环和独立风险层，然后让真实样本决定哪些品种、周期和市场状态值得交易。

## 私人量化与同花顺绑定（2026-08-12）

- 首版确定性策略：`hs300_daily_pullback_v1`，A股多头、日线、沪深300历史成分精选；AI只做解释和复盘，旧AI决策默认 `research_only`，不能生成可执行计划。
- 核心模块：确定性策略、历史股票池、组合风控、策略状态机、事件驱动回测、A股T+1/涨跌停/跳空成交仿真、统一交易生命周期和月度净值统计。
- 本机同花顺目标：远航版 `happ.exe` + 网上股票交易系统5.0 `xiadan.exe`；已验证Win32窗口检测，客户端版本为 `12.1.1.5`。
- 同花顺安全边界：只读同步与安全预填分离；不保存账号密码，不输入验证码，不点击买入、卖出、撤单或最终确认；账户/控件/字段回读任一不确定即阻断。
- 实盘默认保持关闭：账户绑定未确认、预填未开启、`live_trading_enabled=false`；样本外、影子交易和真实费用配置达标后才能由用户显式启用。
- 入口：主窗口“交易管理 → 量化交易工作台 / 同花顺绑定”；工作台展示连接、资金、持仓、委托、成交、量化计划、策略统计和审计记录。

## 新云算力固定股票池（2026-08-13）

- 当前唯一生产股票池版本：`cloud_ai_11_v1`；旧沪深300快照和历史交易仅保留审计/样本外回测，不再参与当前候选。
- 固定 11 股：优刻得-W(688158)、首都在线(300846)、协创数据(300857)、宏景科技(301396)、并行科技(839494)、网宿科技(300017)、润泽科技(300442)、利通电子(603629)、中国电信(601728)、中国移动(600941)、拓维信息(002261)。
- 用户截图分组仅作为研究标签：第一梯队“AI算力租赁”、第二梯队“边缘云/配套”、第三梯队“国资云”，不代表程序已核验其基本面归属。
- 统一日线策略版本：`cloud_ai_daily_pullback_v1`；采用日线趋势回撤预选 + 收盘 15 分钟 4:3:2:1 门槛，不为单只股票复制策略或独立调参。
- 全池按同一“云算力主题”风险桶控制，主题总开放风险上限沿用 0.75%；默认最多 3 个总持仓、每日最多新增 1 个、初始实盘单笔风险 0.25%，且 `live_trading_enabled=false`。
- 并行科技属于北交所，按 30% 涨跌幅制度识别；在行情源、真实费用和券商支持完成确认前仅分析，不允许授权数量。
- 量化交易管理嵌入 PA Agent 当前主窗口，使用工作台页签切换；顶部持续显示连接、评分、风险和下一步操作，不再打开独立交易窗口。
- 固定 11 股无论是否形成日线买点，都持续采集热点与重大公告；股票池摘要显示热点覆盖数量。日线候选仍是进入 15 分钟四层评分的前置条件，热点不能单独创造买点。
- 2026-08-14：四层评分补丁升级为 `1.1.1`，策略重新冻结于 `2026-08-14T15:55:00+08:00`；热点 OOS 数据必须携带 `hotspot_time_window_v2`、发布时间和时间有效性证据。`v2` 修正普通董事/高管减持被误判为重大股东减持的范围错误，并排除已经完成或终止的减持计划。市场情绪中的沪深300宽度独立使用官方300只成分计算，不得复用固定11股池宽度。
