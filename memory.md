# 项目关键信息

- 项目名称：PA Agent（Price Action AI K 线分析桌面端）
- 技术栈：Python 3.11+、PyQt6、pyqtgraph；依赖由 `pyproject.toml` 和 `uv.lock` 管理。
- 推荐本地启动：在项目根目录执行 `uv run python run.py`。
- 主入口：`run.py`，实际 GUI 入口为 `pa_agent.main:main`。
- 本地配置：运行时配置位于 `config/`；仓库仅提交 `*.example.json`，密钥和个人配置不应提交。
- 日志目录：`logs/`，重点查看 `logs/pa_agent.log` 与 `logs/crash.log`。
- 应用性质：本地桌面 GUI，不提供固定 HTTP 端口；运行验收以 GUI 进程、主窗口和启动日志为准。
- AI 后端：支持 OpenAI 兼容 API、Cursor SDK 和官方 `openai-codex` Python SDK；Codex 路由使用本机登录、临时线程和只读沙箱。

## 维护记录

- 2026-08-11：首次在当前工作区进行本地启动检查并创建本文件。
- 2026-08-11：集成 Codex SDK Agent 分析后端，默认 Codex 模型为 `gpt-5.6-terra`。

## 量化交易设计原则

> 不要急着增加更多策略，也不要急着为每个品种单独调参；先修复止损逻辑、建立交易结果闭环和独立风险层，然后让真实样本决定哪些品种、周期和市场状态值得交易。

## 私人量化与同花顺绑定（2026-08-12）

- 首版确定性策略：`hs300_daily_pullback_v1`，A股多头、日线、沪深300历史成分精选；AI只做解释和复盘，旧AI决策默认 `research_only`，不能生成可执行计划。
- 核心模块：确定性策略、历史股票池、组合风控、策略状态机、事件驱动回测、A股T+1/涨跌停/跳空成交仿真、统一交易生命周期和月度净值统计。
- 本机同花顺目标：远航版 `happ.exe` + 网上股票交易系统5.0 `xiadan.exe`；已验证Win32窗口检测，客户端版本为 `12.1.1.5`。
- 同花顺安全边界：只读同步与安全预填分离；不保存账号密码，不输入验证码，不点击买入、卖出、撤单或最终确认；账户/控件/字段回读任一不确定即阻断。
- 实盘默认保持关闭：账户绑定未确认、预填未开启、`live_trading_enabled=false`；样本外、影子交易和真实费用配置达标后才能由用户显式启用。
- 入口：主窗口“交易管理 → 量化交易工作台 / 同花顺绑定”；工作台展示连接、资金、持仓、委托、成交、量化计划、策略统计和审计记录。

## 新云算力固定股票池（2026-08-13）

- 当前唯一生产股票池版本：`cloud_ai_11_v1`；旧沪深300快照和历史交易仅保留审计/样本外回测，不再参与当前候选。
- 固定 11 股：优刻得-W(688158)、首都在线(300846)、协创数据(300857)、宏景科技(301396)、并行科技(839494)、网宿科技(300017)、润泽科技(300442)、利通电子(603629)、中国电信(601728)、中国移动(600941)、拓维信息(002261)。
- 用户截图分组仅作为研究标签：第一梯队“AI算力租赁”、第二梯队“边缘云/配套”、第三梯队“国资云”，不代表程序已核验其基本面归属。
- 统一日线策略版本：`cloud_ai_daily_pullback_v1`；采用日线趋势回撤预选 + 收盘 15 分钟 4:3:2:1 门槛，不为单只股票复制策略或独立调参。
- 全池按同一“云算力主题”风险桶控制，主题总开放风险上限沿用 0.75%；默认最多 3 个总持仓、每日最多新增 1 个、初始实盘单笔风险 0.25%，且 `live_trading_enabled=false`。
- 并行科技属于北交所，按 30% 涨跌幅制度识别；在行情源、真实费用和券商支持完成确认前仅分析，不允许授权数量。
